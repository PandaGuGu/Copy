# 2026-06-23 工作记录

## 运营后台方案定稿

### 现状梳理
当前运营后台仅有 6 个模块：首页轮播、热搜运营、视频审核、专栏审核、动态管理、AI 角色。
**核心缺失**：无用户管理、无系统设置、无评论管理。

### 方案输出
写入 `.workbuddy/memory/MEMORY.md`，包含：

**P0 - 用户管理（待实施）**：
- User 表新增 `Status` / `BannedReason` / `BannedAt` 字段
- 新建 `internal/handler/admin_user.go`：AdminListUsers / AdminGetUser / AdminBanUser / AdminUnbanUser / AdminDeleteUser
- 5 条 admin 路由 + JWT 中间件 banned 检查
- 新建前端 `UserManage.vue`：分页表格 + 状态筛选 + 搜索 + 封禁/解封/详情/注销
- 侧边栏插入「用户管理」在「热搜运营」下方

总计：3 个新建文件 + 6 个修改文件

**P1（后续）- 系统设置**：图形化控制 .env 运行时开关
**P2（后续）- 评论管理**：全局评论搜索/删除

---

## P0 用户管理 — 实施完成

Go/Vite 编译零错误，后端已重启。

### 后端改动
1. **`internal/model/model.go`** — User struct 新增 `Status`(默认active)、`BannedReason`、`BannedAt`（GORM AutoMigrate 自动加列）
2. **`internal/handler/admin_user.go`** — **新建**：
   - `AdminListUsers` — 分页，status/q 筛选，sort 排序，批量加载视频/文章/动态/粉丝数 + 等级
   - `AdminGetUser` — 详情（含统计）
   - `AdminBanUser` — 封禁（Status=banned + BannedReason）
   - `AdminUnbanUser` — 解封（恢复 active）
   - `AdminDeleteUser` — 强制注销（disabled + 1h冷静期）
3. **`internal/handler/router.go`** — 新增 5 条 `/admin/users` 路由
4. **`internal/handler/auth.go`** — Login/Refresh 增加 banned 拦截（CodeAccountBanned）
5. **`internal/errcode/errcode.go`** — 新增 `CodeAccountBanned = 40307`

### 前端改动
1. **`src/pages/admin/UserManage.vue`** — **新建**：el-table 分页表格 + 状态筛选 + 搜索 + 封禁/解封/详情/注销
2. **`src/api/admin.js`** — 新增 5 个函数
3. **`src/router/index.js`** — 新增 `adminUsers` 路由
4. **`src/pages/admin/AdminLayout.vue`** — 侧边栏「热搜运营」下方插「用户管理」

---

## 四模块补全（评论管理 + 系统设置 + 数据仪表盘 + 举报处理）

Go + Vite 编译零错误，已推送。

### 评论管理
- **后端**: `internal/handler/admin_comment.go` — AdminListComments(跨3表联合)/AdminGetComment/AdminDeleteComment
- **前端**: `CommentManage.vue` — 类型筛选/关键词搜索/详情弹窗/强制删除
- 3 条路由 + 侧边栏「评论管理」

### 系统设置
- **后端**: `internal/handler/admin_settings.go` — GET/PUT `/admin/settings`，读写 8 个 .env 开关
- **前端**: `Settings.vue` — el-switch 控件控制视频审核/上传禁用/AI配置等
- 保存时同步更新 .env + 内存 Cfg，即时生效
- 侧边栏「系统设置」

### 数据仪表盘
- **后端**: `internal/handler/admin_dashboard.go` — 总用户/视频/文章/评论/审核队列 + 7日趋势
- **前端**: `Dashboard.vue` — 4 张统计卡片 + SVG 柱状图，设为后台首页 (/admin redirect)
- 侧边栏「数据概览」

### 举报处理
- **后端**: `internal/model/report.go` — Report 模型（reporter/target/status）
- `internal/handler/report.go` — 用户侧 POST /reports + 后台 GET/POST 处理
- **前端**: `ReportManage.vue` — 分页表格/状态筛选/处理/驳回
- 侧边栏「举报处理」

### 侧边栏最终顺序
数据概览 → 首页轮播 → 热搜运营 → 用户管理 → 视频审核 → 专栏审核 → 动态管理 →
评论管理 → 系统设置 → 举报处理 → AI 角色

---

## AI 角色页 LLM 配置功能

完成 `http://127.0.0.1:8888/#/admin/agent` 页面的 LLM 配置功能：

### 需求
- 前端自行配置 Base URL / API Key / Model
- 去除 DeepSeek 品牌，改为通用 LLM 配置
- 进入页面时自动读取并显示当前 .env 配置
- API Key 脱敏显示（前4后4，中间****）
- 编辑时 password 输入框，可切换明文查看
- 无泄漏风险

### 后端改动
1. **`internal/handler/admin_llm_config.go`** — 重写：
   - `AdminGetLLMConfig` — 合并返回 env + DB override 的有效值
   - `AdminPutLLMConfig` — 保存到 DB + 写入 .env 文件 + 更新内存 Cfg
   - `writeEnvFile` — 更新 .env 中 DEEPSEEK_BASE_URL/MODEL/API_KEY
   - 保存时自动同步到内存 Cfg，无需重启即刻生效

2. **`cmd/mini-bili/main.go`** — 修复 agentSvc 初始化语法错误（corrupted lines 189-192）

### 前端改动
1. **`src/api/admin.js`** — 新增 `adminGetLLMConfig` / `adminPutLLMConfig`
2. **`src/pages/admin/AgentManage.vue`** — 新增 LLM 模型配置面板：
   - 查看模式：显示 Base URL / Model / 脱敏 API Key + 来源标签
   - 编辑模式：表单编辑三项配置，API Key 为 password 输入框
   - 顶部统计 "DeepSeek" → "LLM 配置"
   - system_prompt placeholder "DeepSeek system" → "AI system"
   - 新增 ~100 行 CSS 样式

### 编译验证
- Go 后端 build 零错误
- Vite 前端 build 零错误

## 用户违规记录 + 视频举报

- 用户详情从 Drawer 改为 el-dialog 弹窗（620px）
- 用户详情新增违规记录区：被举报次数 + 举报详情列表
- 后端 AdminGetUser 新增 report_count / pending_report_count
- 后端新增 GET /admin/users/:id/violations 返回举报历史
- 视频播放页工具栏新增「举报」按钮，弹窗提交原因 → POST /reports
