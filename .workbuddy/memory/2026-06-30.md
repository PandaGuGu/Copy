# 2026-06-30 工作日志

## 风控模块 6 项收尾修复

### #1 前端下拉标注
RiskManage.vue Category 下拉：设备指纹/行为分析 标 "(即将上线)"

### #2 checkRateLimit 接线
checkRateLimit 从 RiskHitLog 计数 → 改用 risk_rate_counters 表，正确维护 per-window 计数器

### #3 过期名单清理
main.go 新增 goroutine：每 60s 调用 CleanExpiredBWEntries()，删除过期名单条目

### #4 auto_ban 到期解封
main.go 同一个 goroutine：每 60s 检查 ban_expires_at <= now 的用户，status 回 active

### #5 video.go 风控落地
video.go:238 ScanContentRisk 返回值被检查 → 命中 reject/auto_ban 时删除视频并返回 403

### #6 评论审核队列
- backend: admin_comment.go AdminListComments 新增 `status` 参数 (pending/approved)
- frontend: CommentManage.vue 新增「待审」状态筛选下拉

## 4 模块前端补完

### 播放器高级 — PlayerAdvanced.vue
新建 admin 页面：视频ID搜索 → 章节管理(CRUD) + 码率管理(CRUD)，双 tab 布局

### 字幕管理 — 已有
SubtitleManage.vue 已存在且功能完整（列表+创建+编辑+删除+内容预览）

### 评论增强 — 已有覆盖
评论举报由 ReportManage 通用举报模块覆盖；图片评论是用户侧功能；排序配置是 API 查询

### Feed推荐 — 只读 API
订阅源/排行榜/分区推荐均为公开读取端点，无 admin CRUD 需求

### 路由与侧边栏
router/index.js 新增 player-advanced 路由；AdminLayout.vue 侧边栏「内容」分组加「播放器高级」
