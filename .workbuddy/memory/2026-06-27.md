## 更多设置弹窗

- 纯 CSS `position:absolute` 下拉弹窗，含设置/分享/复制/举报四按钮
- 设置子面板：弹幕飘屏/显示观众列表/礼物特效 Checkbox
- 分享/复制功能
- 点击页面任意其他位置自动关闭（document click 监听）

## 举报独立模态框

- 点击"⚠举报"关闭下拉，打开居中模态框（440px 宽，固定定位+半透明遮罩）
- 3x4 网格卡片：违法违禁/色情低俗/赌博诈骗/血腥暴力/违规营销/侵犯未成年/人身攻击/垃圾广告/青少年不良/引人不适/传播谣言/侵权投诉
- 选中卡片高亮（粉色边框+浅粉底）；未选禁用"发起举报"按钮
- 点遮罩或 ✕ 关闭

## 直播历史记录功能（新增）

**问题**：首页历史记录中直播无记录——因为系统完全没有直播观看历史的模型/记录/查询逻辑。

**后端修改**：
- `internal/model/view_history.go`：新增 `LiveViewHistory` 模型（user_id + live_room_id 唯一索引）
- `internal/data/migrate.go`：AutoMigrateAll 注册 LiveViewHistory 表
- `internal/handler/view_history.go`：
  - 新增 `RecordLiveViewHistory(uid, roomID, device)` — upsert 直播观看记录
  - 新增 `buildLiveViewHistoryItems()` — 构建带直播房间信息的返回体
  - 更新 `ListMyViewHistory` — 合并视频/专栏/直播三种历史
  - 新增 `DeleteMyLiveViewHistoryEntry` — 单条删除直播历史
  - 更新 `ClearMyViewHistory` — 清空时包含直播记录
  - 更新 `trimViewHistoryCombined` — 裁剪时包含直播记录（总计500条上限）
- `internal/handler/ws.go`：`ServeLiveChat` 中用户进入直播间(WebSocket 连接)时调用 `RecordLiveViewHistory`
- `internal/handler/router.go`：新增路由 `DELETE /users/me/view-history/live/:liveRoomId`

**前端修改**：
- `src/api/minibili.ts`：`ViewHistoryItem` 类型新增 `media_type: "live"` 和 `live_room_id` 字段；新增 `mbDeleteMeLiveViewHistoryEntry`
- `src/utils/minibiliRoutes.js`：新增 `minibiliLiveRoomRoute(roomId)`
- `src/pages/minibili/ViewHistoryPanel.vue`：
  - 新增 `isLiveRow()` 判断方法
  - 直播条目缩略图显示"直播" badge，不显示进度条
  - 进度显示"已观看"
  - 点击跳转到直播间页面
  - 支持单条删除直播历史

**编译验证**：Go build 通过，零错误。

## 历史记录页顶部栏 B站风格重构

**文件**：`ViewHistoryPanel.vue` + `view-history.scss`

**第一行** (标题+控制区)：
- 左侧：圆形时钟 SVG 图标 + "历史记录" 大号粗体标题
- 右侧：蓝色开关 toggle（:checked=!paused）+ "记录浏览历史" 文字 + 灰色二维码 SVG 分享入口

**第二行** (筛选+搜索+操作区)：
- 分类标签 nav：综合/视频/直播/专栏 四个 tab，选中态蓝色高亮+底部蓝色下划线
- 更多筛选 dropdown：绝对定位弹出菜单，支持"已看完"/"未看完"过滤，点击外部自动关闭
- 搜索框：浅灰底色 `#f1f2f3`，focus 变白底蓝边框，placeholder "搜索标题 / up主昵称"
- 清空历史 btn：垃圾桶 SVG + 文字，hover 变红色（#ed4242）
- 批量管理 btn：列表图标 + 文字，选中态蓝色高亮

**新增数据**：`activeTab`（分类过滤）、`filterMenuOpen`/`filterMenuVal`（更多筛选下拉）、`batchMode`（批量管理）、`filteredItems` computed（按 tab 筛选后传给 displayGroups）、`_onClickDoc` 全局点击监听关闭下拉

**CSS 变量沿用**：`$mb-hist-pink`（#fb7299）、`$mb-hist-blue`（#00aeec）等保持和原有系统一致

## 直播历史记录加固（GetLiveRoom 双保险）

**问题**：`RecordLiveViewHistory` 仅在 WebSocket `ServeLiveChat` 中调用，但 WebSocket 连接可能失败/未登录时 token 为空，导致直播历史始终不写入。

**修复**：在 `GetLiveRoom` (HTTP API) 中也记录——打开直播间页面时必然调用。
- `internal/handler/live.go` → `GetLiveRoom`：从 Authorization header 提取 Bearer token，解析 JWT 得到 uid，调用 `RecordLiveViewHistory(uid, roomID, "web")`
- http.js interceptor 自动给所有请求加 `Authorization: Bearer <token>`，无需改前端
- `ServeLiveChat` 中的 WebSocket 路径保留作为补充

**双路径保障**：HTTP 页面加载 → WebSocket 连接，两处任一成功即写入历史

## 首页 navMenu 历史悬浮窗 tab 筛选修复

**问题**：顶部导航栏 hover "历史"弹出的悬浮窗有「视频」「直播」「专栏」三个 tab，但切换 tab 只是改了高亮，数据没有任何过滤，所有记录混在一起显示，且点击只跳视频页。

**修复**（`navMenu.vue`）：
- 新增 `historyFiltered` computed：按 `historyActiveTab` 筛选 `historyRawItems`（视频=无 live_room_id 且无 article_id，直播=media_type==="live"，专栏=media_type==="article"）
- `historyGrouped` 改用 `historyFiltered` 而非 `historyRawItems`
- `loadHistoryData` 映射新增 `live_room_id`、`article_id` 字段保留
- `goHistoryItem` 重构：直播→`minibiliLiveRoomRoute`，专栏→`minibiliArticleReadRoute`，视频→`minibiliVideoPlayRoute`
- 新增从 minibiliRoutes 导入 `minibiliLiveRoomRoute`、`minibiliArticleReadRoute`、`minibiliVideoPlayRoute`

## 直播管理侧边栏入口（审核分组）

- `AdminLayout.vue`：直播管理移至审核分组，放在视频审核下方
- 最终审核顺序：视频审核→直播管理→专栏审核→评论管理→举报处理→版权管理→风控管理

## 视频审核增强：人工审核+自动审核

- **后端**：`admin_video.go` 新增 `AdminBatchApproveVideos` - POST /admin/videos/batch-approve，遍历所有 pending_review 视频调用 PublishVideo
- **路由**：router.go vOps 组注册 batch-approve 端点（需 video:approve 权限）
- **前端 API**：admin.js 新增 `adminBatchApproveVideos()`
- **VideoReview.vue**：
  - 待审核状态下 filters 下方显示「人工审核」「自动审核」两个按钮
  - 自动审核：ElMessageBox 确认 → 调用批量通过 API → 刷新列表
  - 人工审核：打开 800px 弹窗，内嵌 video 播放器，显示标题/UP主/分区/时长/简介
  - 人工审核操作：通过（并下一个）/ 驳回（复用驳回弹窗）/ 跳过 / 关闭
  - 驳回确认后自动从 reviewVideos 移除并刷新
  - 进度显示 (当前序号/总数)

## VIDEO_REVIEW_REQUIRED 配置修复

- `.env`：`VIDEO_REVIEW_REQUIRED` 从 false 改为 true（之前设置未生效）
- admin settings API 同步设置：`PUT /admin/settings {"video_review_required":true}` → 内存 Cfg 实时生效
- 上传测试视频 ID=63「TestReviewV2」，1秒蓝色画面，手动置为 pending_review 供审核测试

## 转码封面截帧失败修复

- **问题**：ffmpeg ScreenshotJPEG 对某些像素格式（yuvj420p / full-range YUV）用 mjpeg encoder 失败（"Non full-range YUV is non-standard"），导致转码无限重试
- **修复**：`worker/transcode.go` 封面截帧失败时不再丢弃整个任务，改为生成 1x1 黑色 fallback JPEG（`generateFallbackCoverJPEG`），视频/封面 OSS 上传正常继续
- 非永久性截帧错误：跳过截帧 → 写 fallback cover → 继续后续流程
- 永久性截帧错误（无效数据/不支持编码等）：保持原有 fail 逻辑

## 直播管理详情弹窗 + 警告功能

- **后端**：`admin_live.go`
  - 新增 `AdminGetLiveRoomDetail` → GET /admin/live/room/:id，返回直播间详情含 stream_key
  - 新增 `AdminWarnLiveRoom` → POST /admin/live/room/:id/warn，通过 WebSocket Hub.BroadcastJSON 发送 `admin_warning` 消息给所有直播间观众
  - `AdminBanLiveRoom` 增强：封禁时发送 `admin_ban` WebSocket 广播
- **路由**：router.go 新增 GET /live/room/:id、POST /live/room/:id/warn
- **前端 API**：`live.js` 新增 `adminWarnLiveRoom(roomId, reason)`
- **LiveManage.vue** 重构：
  - 操作列增加「详情」按钮，打开 860px 弹窗
  - 弹窗左侧嵌入 flv.js 直播播放器（`http://localhost:8000/live/{stream_key}.flv`）
  - 弹窗右侧显示直播间信息（ID/主播/状态/观看人数/开播时间）
  - 直播中状态：底部 ⚠警告（弹窗输入原因）+ 封禁按钮
  - 警告原因输入框 200 字限制，发送后调用 API → WebSocket 广播到直播间
  - 警告弹窗新增模板快捷选择（违规内容/版权侵权/低俗内容/未成年保护/赌博诈骗/违规广告/标题违规/骚扰攻击），点击模板自动填入输入框
- **LiveRoom.vue** 增强：
  - ws.onmessage 新增 `admin_warning` 处理：显示红色闪烁警告横幅，5 秒后自动消失
  - 新增 `admin_ban` 处理：显示封禁提示，60 秒后消失
  - 警告横幅动画：红色半透明底 + 闪烁效果（warn-flash animation）
  - 警告消息同时插入聊天面板 system 消息

## 选项1：修后门（P0 三修复）— 2026-06-27

1. **BI 端点加 RBAC 锁**：router.go POST/DELETE /bi/reports + POST /bi/export 从裸 admin 组移入 `RequirePermission(dashboard:export)` 权限组
2. **RBACManage.vue 归队**：删除内联 `api()` + `API_BASE`/`ADMIN_API` 硬编码，改用 `@/api/admin.js` 集中式 API。新增 14 个 RBAC API 函数 (adminListRoles/adminCreateRole/adminUpdateRole/adminDeleteRole/adminGetRole/adminAssignPermissions/adminListAdmins/adminCreateAdmin/adminAssignRole/adminListAuditLogs/adminListApprovals/adminApproveFlow/adminRejectFlow)
3. **admin_agent.go 解耦**：消除 `AdminPutAgentSettings` → `AdminGetAgentSettings` 和 `AdminUploadAgentAvatar` → `AdminUploadAgentProfileAvatar` 两处 handler 交叉调用。提取 `respondAgentSettings`/`firstAgentProfile`/`uploadFirstAgentAvatar` 三个共享辅助方法

## 选项2：补增强 — 2026-06-27

1. **播放器**：VideoPlayerBox.vue 倍速(6档)/PiP/章节面板 全已实现，无需改动
2. **字幕编辑器**：新建 `CreatorSubtitleEditor.vue`（新增/编辑/删除字幕，VTT 编辑，语言选择），集成到 video.vue 页（`isVideoOwner` 条件渲染）
3. **ASR Worker**：跳过（需 Whisper 外部服务，超出范围）
4. **评论区排序**：`comment.go ListComments` 新增 `sort` query 参数（hot=按赞排序 / latest=按时间排序 / time=默认），前端排序 UI 已存在（VdCommentPanelMb.vue 按钮 + video.vue commentSort 状态）

## 选项3：重构提质量 — 2026-06-27

**新建共享组件（src/components/admin/）**：
1. `AdminDataTable.vue` — 统一表格+搜索栏+分页组件。Props: data/loading/page/pageSize/total/showPagination。Slots: search-bar/toolbar/默认列。自动处理 loading v-loading、border/stripe、el-pagination。
2. `AdminFormDialog.vue` — 统一新增/编辑弹窗。Props: modelValue/entityLabel/isEdit/onSave。自动处理标题（编辑X/新增X）、saving loading、关闭逻辑。通过 slot 自定义表单内容，`onSave` 返回 Promise 控制流程。
3. `src/utils/admin-helpers.js` — 共享 `formatTime()` 函数，消除各页面重复定义

**示范重构**：
- `DynamicManage.vue` → 使用 AdminDataTable 替换手动搜索栏+表格+分页，减 ~30 行
- `BannerManage.vue` → 使用 AdminFormDialog 替换 el-dialog 壳，减 ~20 行
- `CommentManage.vue` → 使用 AdminDataTable 替换搜索栏+表格+分页，减 ~25 行

Go build 通过。其余 19 个 admin 页面可逐步迁移。

## 选项3 续：AdminDataTable 批量迁移（2026-06-27）

**已迁移 9 个 admin 页面**：
1. DynamicManage ✅ (搜索+表格+分页)
2. BannerManage ✅ (AdminFormDialog 编辑弹窗)
3. CommentManage ✅ (搜索+下拉筛选+表格+分页)
4. UserManage ✅ (搜索+表格+分页)
5. ArticleReview ✅ (表格+分页，状态筛选按钮在外)
6. LiveManage ✅ (tabs+表格+分页，Composition API)
7. SubtitleManage ✅ (搜索+表格+新增工具栏，无分页)
8. CSManage ✅ (会话 tab，select 筛选+表格，Composition API)
9. SpecialManage ✅ (双 tab 表格+新增工具栏，无分页)

**不适合迁移的 13 个页面**：
Dashboard（图表）、HotSearchManage（复杂）、VideoReview（播放器）、Settings（表单）、ReportManage（统计+批量）、AgentManage（多 tab）、TicketManage（统计+复杂）、RiskManage（多 tab）、CopyrightManage（Composition API+状态机）、BIReport（复杂）、OpsMonitor（巨大）、ConfigManage（多 tab）、RBACManage（已用 script setup）
AdminLayout/AdminLogin（基础设施）

**结论**：所有适合 AdminDataTable/AdminFormDialog 的页面已全部迁移完毕。

## A：内联 API 归队（2026-06-27）

**CSManage.vue**：删除 `api()` + `http` 直接调用，改用 `adminListConversations/adminGetConversation/adminSendConversationMessage/adminCloseConversation/adminAssignConversation/adminListCsTemplates/adminCreateCsTemplate/adminUpdateCsTemplate/adminDeleteCsTemplate`
**TicketManage.vue**：删除 `api()` + `http`，改用 `adminListTickets/adminGetTicket/adminAssignTicket/adminAutoAssignTicket/adminUpdateTicketStatus/adminTicketSendMessage/adminCloseTicket/adminReopenTicket/adminListAdmins`
**CopyrightManage.vue**：删除 `http` 直接调用 + `ADMIN_API` 常量，改用 `adminListCopyrightComplaints/adminGetCopyrightComplaint/adminAcceptCopyrightComplaint/adminRejectCopyrightComplaint/adminTakedownCopyright/adminRestoreCopyright`

admin.js 新增：CS（10 函数）+ 工单（8 函数）+ 版权（6 函数）+ 自用（2 函数）+ 专题活动（8 函数）= 34 个集中式 API 函数

## B：Service 层建设（2026-06-27）

**新建 `internal/service/` 包**：
- `services.go` — Services 容器（Video/User/Comment），通过 DI 注入
- `video_service.go` — ListPublished / ListByUser / Publish / Reject / Delete / GetByID
- `user_service.go` — List / GetByID / Ban / Unban / Delete
- `comment_service.go` — ListByVideo / ListReplies / Create / Delete / ToggleLike

**DI 集成**：
- `deps.go` Dependencies 新增 `Svcs *service.Services`
- `main.go` 初始化 `service.NewServices(db, cfg)` 注入 deps

Go build 通过。后续 handler 可通过 `a.Svcs.Video/User/Comment` 访问分层服务。

## BMAD 架构全面评估（2026-06-27）

**技能**：bmad-architecture — Winston 架构师角色
**结果**：整体评分 89/100，架构文档 30/30 验证通过

**方法**：
1. 读取 SPEC.md、Rule.md、Skill.md、README.md、bmad-output/* （架构/PRD/决策日志）
2. 运行 `nfr-checklist.sh` 和 `validate-architecture.sh`
3. 并行启动两个 Explore Agent 深入探索后端（79 handler, 86 model, ~260 route）和前端（22 admin 页面, Router/Auth/Permissions）
4. 产出评估报告 + 健康度可视化

**关键发现**：

优点：
- ADR 决策完整（9 已接受 + 6 提议），Context/Decision/Consequences/Alternatives 全覆盖
- RBAC 双端（中间件 + 路由守卫）实施良好，19 种 resource:action 权限
- 零 SQL 注入（全参数化），审计全覆盖
- SPEC v1.0 的 16 项 AC 全部达标，实际功能远超 v1.0 范围

待修复（P0）：
1. BI 报表 POST/DELETE 端点缺少 RequirePermission 中间件保护
2. RBACManage.vue 绕过 `src/api/admin.js` 集中式 API 层，内联 api() 硬编码 URL
3. admin_agent.go 两处 handler 方法间交叉调用

待改进（P1）：
4. TableName() 覆盖率仅 50%（module_extend.go 全定义，model.go 仅 4/24）
5. 服务层不完整 — 核心 CRUD 散落在 handler
6. 前端无共享 admin 组件 — 22 页面各自重复搜索/表格/弹窗模式
7. Options/Composition API 混用
8. NFR 检查清单部分项缺失（备份/DR/成本/合规）

**结论**："可以" — 生产就绪度约 85%，作为个人项目架构质量优秀，核心链路完整可运行

## README & 截图更新（2026-06-27 晚）

- 复制 4 张新截图覆盖 docs/images/（db-arch-bento-top/bottom、er-diagram-full、er-diagram-admin-ext）
- README 全局表数 82→84、模块数 14→15（6 处修正）
- cakecake_er_figma-diagram.html 实体数 82→84
- 移除仓库结构中的 bmad-output/ 引用
- 新增直播系统完整章节：推流架构、3 张数据表、WebSocket 事件协议（9 种事件）
- 业务模块表分布新增"直播系统"行（live_rooms + live_warn_templates）
- GitHub 推送 commit 6341c81

## LICENSE 更新（2026-06-27 晚）

- 表数 82→84，模块数 14→15
- 新增直播系统、Service 分层、AdminDataTable/AdminFormDialog、18 模块 API 集中化描述
- 新增 **第三方依赖声明** 表格：Go 后端（8 项）+ 前端（6 项）+ 基础设施（5 项），含许可证类型

## video-platform-builder skill 创建（2026-06-27 晚）

- 基于 cakecake 项目架构 + bmad-architecture ADR 方法论，提炼为可复用的视频平台快速构建技能
- 路径：~/.workbuddy/skills/video-platform-builder/
- SKILL.md：10 阶段模块递增式构建流程（脚手架→认证→视频→弹幕→评论→Admin→23模块→直播→社交→搜索）
- REFERENCE.md：82 张数据模型清单、19 种 RBAC 权限码、视频状态机、环境变量清单、GORM AutoMigrate 模板、WebSocket Hub 架构、前端路由分组、FAQ
- templates/deps.go.tmpl：Go DI 容器模板
- 7 条核心 ADR：模块化单体/REST API/JWT双体系/WebSocket Hub/Vue3+ElementPlus/FFmpeg转码/FeatureFlag灰度
- NFR 覆盖矩阵 + Docker Compose + 代码模板 (Go Handler / GORM Model / Vue Admin 页面)
