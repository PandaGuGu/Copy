# 2026-06-25 工作日志

## 修复：Admin 后台侧边栏切换组件不刷新（深度排查）

**问题**：点击"数据报表"后再点击其他左侧管理按钮，URL 变化但右侧组件不刷新。

**根因**：Vue 3 (3.5.13) + Vue Router 4 (4.5.0) + createWebHashHistory 组合下，嵌套 `<router-view>` 在首次 hash 导航后无法响应后续路由变化。经深度排查：
- `:key="$route.fullPath"` 无效
- `$forceUpdate()` 无效
- `beforeRouteUpdate` 无效
- `defineAsyncComponent` + `<component :is>` 无效
- `<keep-alive>` exclude 无效（去掉 keep-alive 也不行）
- 纯 `<a href="#/...">` 标签导航也无效
- `location.hash = '...'` 能改变 URL 但 RouterView 仍不更新

唯一可靠的修复：`location.hash + location.reload()` 强制整页重载。

**修复内容**：
1. `AdminLayout.vue`：侧边栏改用 `<a href="javascript:;" @click="navigate(name)">`，`navigate()` 方法执行 `location.hash = '#/admin/xxx'; location.reload()`
2. `BIReport.vue:349`：修复 `let r; ows = []` → `let rows = []` 语法错误
3. `App.vue`：恢复原始 keep-alive 配置

**代价**：每次侧边栏点击会触发整页刷新（有闪烁），但能保证正确渲染。

---

## 修复：RBAC 权限审计页面 404 错误

**问题**：跳转权限审计页面后显示 "Request failed with status code 404"

**根因**：前后端 API 不匹配，共 7 处问题：
1. **P0** — 后端缺少 `GET /rbac/admins` 路由（`AdminListAdmins` handler 已实现但未注册）
2. **P0** — 前端调用 `/rbac/approvals`，后端路由为 `/rbac/approval-flows`
3. **P1** — 权限分配 `method: 'PUT'` 应为 `method: 'POST'`
4. **P1** — 角色分配 `method: 'PUT'` 应为 `method: 'POST'`
5. **P2** — 权限载荷不匹配：前端发 `permissions` (code 字符串数组)，后端期望 `permission_ids` (数字 ID 数组)
6. **P2** — 角色权限弹窗取值：`permissionForm.role = d` → 应为 `= d.role`
7. — `AdminListAdmins` 不返回角色信息

**修复**：
- `router.go`：新增 `GET /rbac/admins` + `GET /rbac/roles/:id` 路由
- `admin_rbac.go`：`AdminListAdmins` 增加角色关联查询；`AdminAssignRolePermissions` 支持 `permissions` code 字段
- `RBACManage.vue`：修正 6 处接口调用

---

## AdminLayout 页面切换过渡动画优化

**问题**：`navigate()` 使用 `location.hash + location.reload()` 全页重载，侧栏切换白屏闪烁、体验生硬。

**修复内容**：
1. `navigate()` → `this.$router.push({ name })`，利用 SPA 路由系统，去除 `location.reload()`
2. `<router-view>` 包裹 `<transition name="adm-page" mode="out-in">`，内容区先出后入
3. CSS 动画：`enter-from: translateX(12px) + opacity:0`，`leave-to: translateX(-12px) + opacity:0`，200ms ease
4. 侧栏 `.adm-side__item` 加 `transition: color/background/border-color 0.2s`
5. 移除无用的 `$route.fullPath` watcher

**已推送到 GitHub**：commit `8d419e5`

---

## 侧栏分组重构（6 大功能群组）

基于用户提出的 6 大分组方案：数据 → 运营 → 审核 → 用户 → AI → 系统，重构侧栏为可折叠手风琴式导航。

**改动**：
- 侧栏宽度 160px → 190px
- 21 个平铺菜单 → 6 个可折叠分组，每组带图标 + 箭头指示
- 点击分组标题展开/折叠，`v-if` + `<transition name="adm-group-slide">` 实现滑入滑出动画
- 当前路由所在分组自动展开，路由切换时自动同步展开状态
- 子项缩进 `padding-left: 40px`，视觉层级清晰
- 箭头 `transform: rotate(-180deg)` 动画，展开时旋转 180°

**已推送到 GitHub**：commit `491c3b0`

---

## 修复：RBAC 权限越权问题 — cs_admin 等可越权操作

**问题**：cs_admin（客服管理员）、content_review（内容审核）等管理员可以看到并操作其角色不应访问的模块。

**根因**：两层的系统性问题——

### 后端（router.go）
- 仅有 5 组路由有 `RequirePermission` 中间件（user.ban / video.approve / comment.delete / ticket.handle / article.approve）
- 其余 90%+ 的管理路由（热搜/轮播/AI/LLM/设置/播放器/字幕/工单/风控/版权/BI/客服/运维/配置发布/RBAC/专题/动态）**零权限检查**，任何登录 admin 均可操作

### 前端（AdminLayout.vue + router/index.js）
- 侧边栏仅做视觉过滤（`visibleGroups`），不具安全意义
- 所有 admin 路由无 `beforeEnter` 守卫，知道 URL 可直接跳转

**修复内容**：

**后端 router.go**：给 17 个模块补上 `RequirePermission` 中间件：
| 模块 | 子组 | 权限码 |
|---|---|---|
| 动态管理 | dynOps | `dynamic:manage` |
| 首页轮播 | banOps | `banner:manage` |
| 热搜运营 | hsOps | `hotsearch:manage` |
| AI角色 | agOps | `agent:manage` |
| LLM+系统设置 | setOps | `setting:manage` |
| 播放器章节/码率 | chOps | `video:approve` |
| 字幕管理 | subOps | `subtitle:manage` |
| 工单系统 | ticketOps | `ticket:handle` |
| 风控管理 | riskOps | `risk:manage` |
| 版权管理 | crOps | `copyright:handle` |
| BI报表 | biOps | `dashboard:export` |
| 客服后台 | cusOps | `cs:manage` |
| 评论举报 | cmrOps | `comment:delete` |
| 运维监控 | opsOps | `ops:manage` |
| 配置发布 | cfgOps | `config:manage` |
| 权限审计 | rbOps | `rbac:manage` |
| 专题活动 | speOps | `special:manage` |

保留 `/rbac/me/permissions` 对所有 admin 开放（前端侧栏权限查询用）。

**前端**：
- `adminAuth.js`：新增 `getAdminPerms()` / `setAdminPerms()` localStorage 缓存
- `router/index.js`：21 个 admin 子路由全部加 `meta.perm`；全局 `beforeEach` 增加权限校验（perms 非空时拦截无权限路由 → 跳转仪表盘）
- `AdminLayout.vue`：`fetchPerms()` 同步写 `setAdminPerms()` 到 localStorage

---

## 修复：侧栏权限过滤失效（"还是能看到"）

**问题**：cs_admin 等仍能看到无权模块的侧栏菜单项。

**根因**：
1. **种子数据** — `SeedRBAC` 对已有管理员 `continue` 跳过，不分配角色 → cs_admin 无权限，`AdminGetMyPermissions` 返回空数组
2. **前端状态区分** — `visibleGroups` 以 `perms.length === 0` 判断"加载中"，空权限与加载中无法区分 → 空 perms 时展示所有模块

**修复**：
- `rbac_seed.go`：已有管理员不再 skip，改为 `FirstOrCreate` 角色分配。
- `AdminLayout.vue`：新增 `permsLoaded` 标志位，`visibleGroups` 用其区分"加载中/已加载"。
- `fetchPerms()` catch 中显式设 `this.perms = []`，无论成败均设 `permsLoaded = true`。

**构建**：
- `go build -o mini-bili.exe ./cmd/mini-bili/` ✅
- `vite build` → dist/ ✅

## P0 四大模块反馈闭环补全

针对版权/风控/客服/工单四个模块的反馈环断裂问题，实施 P0 修复：

**核心策略**：复用已有 `ChatHub.PushJSON()` WebSocket 推送能力 + 已有 `sendNotification()` 函数，补全调用点。

**版权管理** (`admin_copyright.go`)：
- `sendNotification()` 增加 ChatHub 实时推送
- `AdminRejectCopyrightComplaint` → 通知投诉发起人
- `AdminTakedownContent` → 通知内容所有者
- `AdminRestoreContent` → 通知内容所有者
- 新增 `getContentOwnerID()` 辅助函数

**风控管理** (`admin_risk.go`)：
- `ScanContentRisk` 重构：分操作类型通知
- `notifyRiskHit()` — 告知用户内容被拦截/隔离
- `notifyAdminRiskAlert()` — 通过 ChatHub 向管理员推送实时告警
- 新增 `min()` 辅助函数

**客服后台** (`admin_cs.go`)：
- `AdminSendCSMessage` → 推送 `cs_new_message` 到用户 ChatHub
- `AdminCloseCSConversation` → 推送 `cs_conversation_closed`
- 新增 `pushCSNotification()`, `truncateText()` 辅助函数

**工单管理** (`admin_ticket.go`)：
- `AdminUpdateTicketStatus` → 推送 `ticket_status_xxx`
- `AdminAddTicketMessage` → 推送 `ticket_new_reply`
- `AdminCloseTicket` → 推送 `ticket_closed`
- 新增 `pushTicketNotification()`, `statusLabel()` 辅助函数

**Go 编译通过**。

---

## BMAD 架构回溯：运营中心架构文档

使用 `@skill:bmad-architecture` 对运营中心系统（23 模块）进行架构回溯分析。

**三步流程**：
1. **逆向 PRD** — 从 24 个 admin handler + router.go 提取全部 FR（FR-001~025）和 NFR（AUTH/DATA/PERF/CONFIG/EXT 5 大类 15 项），写入 `bmad-output/prd.md`
2. **锁定 ADR** — 提取 9 条架构决策记录（REST+JSON、MySQL+GORM、独立 JWT、RBAC、模块化单体、全审计、统一错误码、FNV-1a 灰度、多级审批流），写入 `bmad-output/decision-log.md`
3. **生成架构文档** — 填充 template + FR/NFR 覆盖矩阵（30/30 全部"已处理"，4 项"部分"缺前端 UI），`validate-architecture.sh` 30/30 通过，0 失败 0 警告

**关键结论**：当前架构已经正确——模块化单体 + 文件级模块拆分 + RBAC 细粒度权限 + 全操作审计。主要问题是"决策未文档化"而非"架构错误"。

**产出**：
- `bmad-output/prd.md` — 25 FR + 15 NFR
- `bmad-output/decision-log.md` — 9 ADR + RBAC 权限映射表
- `bmad-output/architecture.md` — 完整架构文档（验证通过）

---

## P0/P1/P2 缺口补全

审计后的缺口验证与修复：

**已存在（审计误判）：**
- P0: VideoPlayerBox 倍速/PiP/章节/码率 — 代码中已完整实现
- P0: VdCommentPanelMb 评论排序/过滤 — 代码中已完整实现  
- P1: SubtitleEdit.vue — 已存在
- P1: CreatorDashboard.vue — 已存在
- P2: ASR Worker — `internal/worker/subtitle_asr.go` 已完整实现

**本次修复 / 新增：**
- P2: `SpecialPage.vue` — 新建公开专题展示页，路由 `/minibili/special/:slug`，渲染 Blocks JSON 内容
- P2: `router/index.js` — 注册专题页路由
- P2: `subtitle.go` — 新增 `RequestSubtitleASR` handler（`POST /videos/:id/subtitles/asr`）
- P2: `router.go` — 注册 ASR 路由
- P2: `main.go` — 启动 SubtitleASRWorker（mock 后端）

---

## NocoBase 尝试 + Go 原生工单引擎

NocoBase v2.1.11 在 Windows 下 npm/pnpm 均超时失败，切换为 Go 原生实现：

**工单增强 (`admin_ticket.go`)：**
- `AdminAssignTicket` → 分配时自动设 SLA 截止时间（+2h）并推送通知
- `AdminAutoAssignTicket` → 轮询最少负载管理员自动分配（`POST auto-assign`）
- `AdminGetSatisfactionStats` → 满意度看板（总平均分/分布/按管理员分组）
- `AdminGetTicketStats` → 工单仪表盘（各状态计数/超时/本月满意度/周响应时间）

**风控告警 (`admin_risk.go`)：**
- `AdminGetRiskStats` → 风控趋势（总命中/TOP10规则/日趋势折线）

**桥接 API (`nocobase_bridge.go`)：**
- 6 个内部端点备用（视频下架/恢复、用户封禁/解封、用户/视频信息查询、webhook）
- NocoBase 配置项已加入 `.env`

Go 编译通过。

---

## 修复：工单指派/更新状态 400 错误

**问题**：点击指派和更新状态报 `Request failed with status code 400`。

**根因**:
1. 后端 `AdminGetTicket` 返回 `{ticket: {...}, messages: [...]}` 包裹了一层，但前端直接访问 `detail.id` / `detail.title` 等字段 → `detail.value.id` 为 undefined → URL 变成 `/tickets/undefined/assign` → 400
2. 字段名不匹配：后端用 `subject` / `reporter`，前端期望 `title` / `user`
3. 统计字段：后端返回 `assigned_count`，前端用 `processing_count`
4. 管理员下拉列表：后端只返回 `display_name`，前端用 `nickname` 渲染

**修复文件**:
- `internal/handler/admin_ticket.go`:
  - `AdminGetTicket`: 从 `{ticket: {...}}` 改为扁平结构，`subject`→`title`，`reporter`→`user`
  - `AdminListTickets`: item 字段 `Subject`→`Title` / `Reporter`→`User`，stats `assigned_count`→`processing_count`
- `internal/handler/admin_rbac.go`:
  - `AdminListAdmins`: 增加 `nickname` 字段（值取 `display_name`）

**验证**: `go build` 通过。

---

## 新增：首页客服按钮 + /customer-service 页面

**文件**:
- `cakecake-vue/.../CustomerService.vue` — 新建客服中心页面
- `cakecake-vue/.../router/index.js` — 新增路由 `/customer-service`
- `cakecake-vue/.../elevator/elevator.vue` — 悬浮小电视上方添加"客服"按钮

**CustomerService 页面功能**:
- 4 种工单类型卡片（举报/版权/账号/其他）弱引导选择
- 选类型后展示快捷表单，直接提交工单（POST /api/v1/tickets）
- 我的工单列表 + 点击查看详情（消息线程 + 补充回复）
- 工单已解决时弹出 5 星满意度评分（POST /api/v1/tickets/:id/satisfaction）
- 评分后工单自动关闭

**验证**: `npx vite build` 通过。
