# 2026-06-29 工作日志

## 项目恢复
- **问题**：启动 Cakecake 项目失败，`mini-bili.exe` 丢失 + 前端 `node_modules` 丢失
- **修复**：
  - Go 编译：`GOPROXY=https://goproxy.cn,direct go build -o mini-bili.exe ./cmd/...`（默认 proxy.golang.org 被墙）
  - 前端：`C:\Users\Administrator\Desktop\cakecake-project\cakecake-vue\bilibili-vue\` 下 `npm install`
- **结果**：后端 8080 ✅ + 前端 8888 ✅ 均已恢复运行
- **注意**：启动脚本 `C:\Users\Administrator\Desktop\启动Cakecake.ps1` 路径已正确指向 C 盘项目目录，无需修改

## 动态管理后台增强 — 统一动态视图

### 背景
用户反馈 `/admin/dynamic-manage` 看不到 `/minibili/up/3?nav=dynamic` 的内容。
根因：UP 个人页"动态"tab 是三类合并（视频+专栏+图文动态），而管理后台只查 `user_dynamics` 一张表。

### 改动清单（6 个文件）

| 文件 | 改动 |
|------|------|
| `internal/model/user_dynamic.go` | 新增 `Type` 字段（`text`/`image`），默认 `'image'` |
| `internal/data/migrate.go` | 新增 `Type` 列迁移 + 回填（有图片→image，无→text） |
| `internal/handler/admin_dynamic.go` | `AdminListDynamics` 支持 `user_id`/`type` 过滤；`AdminDeleteDynamic` 写入 AuditLog；新增 `AdminListUnifiedDynamics` 三表 UNION（videos+articles+user_dynamics）支持 `user_id`/`kind`/`q` 过滤 |
| `internal/handler/user_dynamic.go` | `PostUserDynamic`/`PutMyUserDynamic` 自动判定 `Type`；`userDynamicPayload` 返回 `type` 字段 |
| `internal/handler/router.go` | 注册 `GET /api/v1/admin/dynamics/unified` 路由 |
| `cakecake-vue/.../api/admin/dynamic.js` + `admin.js` | 新增 `adminListUnifiedDynamics` 导出 |
| `cakecake-vue/.../DynamicManage.vue` | 对接 unified 端点；搜索栏增加用户ID输入+类别下拉（全部/视频/专栏/图文/纯文字）；表格新增"类别"列（彩色标签）；详情弹窗适配不同 kind |

### 当前数据
- videos (published): 33 条（用户3: 13）
- articles (published): 7 条（用户3: 7）
- user_dynamics: 0 条

## BI 统计报表改造 — ECharts + 渐变卡片

### 调研
浏览 GitHub 20+ Vue3 管理后台项目（Art Design Pro / Geeker-Admin / Vue Vben Admin 等），确定 ECharts 5 为最佳图表库方案。

### 新增依赖
- `echarts` + `vue-echarts`（BIReport chunk: ~385KB gzipped）

### 新增文件
| 文件 | 用途 |
|------|------|
| `src/composables/useECharts.js` | ECharts 生命周期 composable（init/resize/dispose） |
| `src/components/admin/BiChart.vue` | 通用图表组件，封装 ECharts init + 深监听 option + resize |
| `src/components/admin/BiCard.vue` | 彩色数据卡片，支持 label/value/trend/icon/color（左侧 3px 彩色边框 + hover 动效） |

### 改造 BIReport.vue
- 所有手写 SVG → ECharts（横向柱状图/环形饼图/平滑折线面积图/多系列折线图）
- 数据卡片 → BiCard（blue/teal/purple/amber/pink 彩色边框）
- 分区统计新增环形饼图（与柱状图并排）
- 时序折线增加 areaStyle 渐变填充
- 删除所有 `chartW` / `tsChartH` / 手写 `polyline` 计算逻辑，代码量大幅减少
- 前端编译通过 ✅
