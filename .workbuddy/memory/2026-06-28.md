# 2026-06-28

## 架构文档全面重评估（P0→P1→P2→P3）

**触发**: 用户要求用 bmad-architecture 重新评估全部（非仅 NFR 第4项）

### P0 — 事实性错误修复（8项）
- §1 范围：直播从"不在范围"移入范围，补充社交/搜索/Service层到范围列表
- §1 API 计数：62+ → 80+ admin + 20+ auth + 5+ public + 用户端
- §5 数据模型：补全 v1.0 核心实体（11表）、社交/直播/搜索实体（11表）
- §10 部署：修正为 Windows本地 + docker-compose + Linux ECS（不再说"只有Linux生产"）
- §7 覆盖缺口：区分 admin CRUD（已就绪）vs 用户端增强（部分）
- §11 路线图：FR-050 直播从 P3"待实施"→"已实施"，NFR-LIVE-1/2→"已处理"
- §11 差距全景：直播 ❌→✅
- 文档版本 1.1→1.2→2.0

### P1 — 结构缺失补全（8项）
- §2 新增 Service 层架构（三层解耦描述）
- §2 新增 WebSocket 架构（弹幕/直播/私信三通道）
- §2 新增 ES 搜索架构（索引策略+热搜运营+搜索历史）
- §4 新增用户端核心组件（VideoPlayer/SocialSystem/LiveStreaming）
- §4 新增前端共享组件体系（AdminDataTable/AdminFormDialog/18模块API）
- §5 数据模型（已在P0完成 v1.0+社交/直播/搜索实体）
- §6 新增用户端 API（35+ 端点，覆盖认证/视频/弹幕/评论/社交/直播/搜索）
- §5 Redis 策略细化（播放量/Token黑名单/弹幕PubSub/热搜词/搜索历史）

### P2 — NFR 全面覆盖（25+条目）
新增 NFR 覆盖矩阵行：
- NFR-REL-1~5（备份/RPO=24h,RTO=4h/健康检查/降级/错误率）
- NFR-AVAIL-1~3（99%正常运行/恢复验证/监控告警）
- NFR-SEC-1~4（HTTPS/bcrypt/输入验证/限流）
- NFR-OBS-1~5（结构化日志/链路追踪/指标/日志聚合/错误追踪）
- NFR-COMP-1~4（数据保留/审计不可篡改/删除权/脱敏）
- NFR-COST-1~3（≤500CNY/OSS生命周期/规格规划）
- NFR-DI-1~3（事务/外键/一致性）
- NFR-MAINT-1~2（编译验证/代码风格）
- NFR-UA-1~2（浏览器兼容/中文界面）

NFR 覆盖率：49% → ~85%

### P3 — 细节修正
- 组件图升级：加入 Service Layer + User Handlers + Elasticsearch
- 文档状态：草稿 → 已发布（v2.0，对标 SPEC v2.0）

### 验证
- 所有改动后运行 validate-architecture.sh：30/30 通过
- ADR 引用从 118 → 133

## 推荐引擎方案文档化（暂不实施）

- `docs/architecture.md`：
  - §2 新增「推荐引擎架构」— 四层漏斗(召回→粗排→精排→重排)、离线计算(ItemCF Cosine)、在线服务(Redis)、冷启动策略、评估体系
  - ADR-016 新增（ItemCF 协同过滤推荐引擎，状态：提议）
  - FR-036 / NFR-REC-1/2 状态：待实施 → 设计中
  - ADR-015 直播状态：提议 → 已实施
  - 新增重新审视触发条件 2 条
- `SPEC.md` v2.0 → v2.1：
  - F17 新增：推荐引擎一期(ItemCF + 多路召回 + 粗排 + 重排) + 二期(排序模型升级)
  - AC-25 新增验收标准
