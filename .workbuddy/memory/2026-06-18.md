# 2026-06-18 工作记录

## 首页布局调整（B站风格）— 第二轮修复
- 用户截图反馈首版布局不像，需要：左侧大图更大(660×380)，右侧2列3行6小卡，底部5×3刷新网格
- 修改6个文件：
  1. `slide.vue`：轮播图 640×220 → 660×380，transition translateX 更新
  2. `recommend.vue`：网格 3×2(100px) → 2×3(1fr)，高度 220→380，卡片高改为 100% 自适应，card-mark 改为 bottom:0 定位
  3. `videoRecommendFeeds.js`：HOME_RECOMMEND_PAGE_SIZE 8 → 6
  4. `storeyBox.vue`：5列 grid 布局（padding-bottom 62.5%），显示 15 条/3行，ps 10→15
  5. `App.vue`：`.l-con` 宽度 900px → calc(100%-260px) 自适应
  6. `home/index.vue`：初始 setDynamicRegion ps 10→15
- 两侧继续保持 60px padding，max-width 1160px 居中
- 验证通过：slide=660×380, recommend=380×380, grid=2列3行(180×120), storeyBox=5列grid
## 首页比例微调（第三轮）
- 右侧改为 3列×2行，大图宽 = 小图宽的 1.5 倍（624 = 1.5 × 416）
- slide: 660 → 624, 保持 380
- recommend-module: calc(100%-624px) = 416px
- recommend-grid: 3列2行, gap 8px, padding-left 0 (靠边)
- 提交：486a472（GitHub 暂时连不上，待网络恢复后推送）
