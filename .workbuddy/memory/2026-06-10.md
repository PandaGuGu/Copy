# 2026-06-10 部署记录

## 完成事项
1. 克隆仓库 `earthcake2233/cakecake` 到 `cakecake-project`
2. 安装 Go 1.24.5（winget 安装失败，通过 golang.google.cn 下载 MSI 安装成功）
3. 修改 `go.mod`：`go 1.25.0` → `go 1.24.0`，`golang.org/x/time v0.15.0` → `v0.5.0`
4. 通过 `goproxy.cn` 代理下载 Go 依赖，编译后端成功 (`mini-bili.exe`)
5. 安装 Redis Windows 版 (winget)，启动成功 (port 6379)
6. 安装 Erlang OTP 26.2.5（替换不兼容的 Erlang 29）+ RabbitMQ 4.3.0 zip 解压到 `C:\rabbitmq`
7. RabbitMQ 启动成功！(Erlang 26 + RabbitMQ 4.3.0, port 5672 AMQP + 15672 管理界面)
8. 创建 `.env` 配置文件 (MySQL root:123456, 数据库 minibili)
9. 后端启动成功 (port 8080, health check 200 OK, 已连接 RabbitMQ, `mini_bili_transcode` 队列创建)
10. 安装前端依赖 (npm install)，创建 `.env.development`
11. 前端启动成功 (port 8888)
12. 设置 ERLANG_HOME 环境变量为 `C:\Program Files\Erlang OTP`
13. 创建 `data/tmp` 目录（视频临时上传路径）
14. `main.go` RabbitMQ 保留可选模式（连接失败时 warn 而非 fatal）
15. `$ProgressPreference='SilentlyContinue'` 大幅加速 PowerShell Invoke-WebRequest 下载
16. 安装 FFmpeg 8.1.1 (winget: Gyan.FFmpeg.Essentials)
17. 配置阿里云 OSS（Bucket: pandagugu, 中国香港）
18. 启用视频上传 (`VIDEO_UPLOAD_DISABLED=false`)
19. 删除本地 AccessKey.csv 防止泄露

## 关键决策
- Erlang 29 不兼容 RabbitMQ 4.3.0 → 降级到 Erlang 26.2.5 解决
- OSS 视频存储已启用（pandagugu / 中国香港）
- ES/DeepSeek 未配置（可选）
- 视频上传功能完整可用（OSS + FFmpeg + RabbitMQ 转码链路就绪）
- 管理员账号自动创建 (admin/admin123)
