# 2026-06-26 运维监控模块修复

## P0: threshold 类型不匹配
- OpsMonitor.vue `threshold: ''` (string) → `threshold: 0` (number)
- el-input → el-input-number
- 修复后新建告警规则不再因类型错误失败

## P1: N+1 查询
- admin_ops.go AdminListAlertRecords 批量预加载 AlertRule
- 原来每条记录单独查一次 → 现在一次 IN 查询 + map 查找

## P1: 全 tab 分页
- 任务队列、告警记录、链路追踪 3 个 list 各加 el-pagination
- 添加 taskPage / alertPage / tracePage 分页状态
- fetchTasks / fetchAlerts / searchTraces 传 page+page_size

## P2: OSS/MQ 健康检查去假阳性
- storage/oss.go 新增 `Ping(ctx)` 方法（ListObjects MaxKeys=1）
- queue/rabbitmq.go 新增 `IsAlive()` 方法（conn.IsClosed + ch.IsClosed）
- admin_ops.go 健康检查改为真探测（OSS 5s 超时，MQ 连接状态检查）

## 附带修复
- CDN 状态标签补全 processing/success
- toggleAlertRule 改用 POST /toggle 端点
- 链路追踪进入 tab 自动加载

## 种子数据清理 + 真实任务接入
- DELETE task_logs 中 3 条虚假种子数据 (backup_database/hot_search_refresh/daily_video_stats)
- subtitle_asr.go: worker 新增 TaskLog 跟踪（running→success/failed 生命周期）
- admin_ops.go AdminTriggerSync: 移除 ErrorMsg 存 sync_type 的语义滥用

## 真实任务类型清单
| task_type | 触发入口 | 状态 |
|-----------|---------|------|
| transcode | video/draft 上传 → RabbitMQ → transcode worker | ✅ 已有 |
| subtitle_asr | RequestASR → SubtitleASRWorker | ✅ 新增 |
| report_export | BI报表 → AdminExportReport | ✅ 已有 |
| sync | AdminTriggerSync ES/播放量同步 | ✅ 已有 |

## 收尾：任务队列统计面板 + 同步按钮 + 健康总览
- OpsMonitor.vue 任务tab: 顶部 4 格队列统计卡片 (pending/running/failed/retrying)
- OpsMonitor.vue 任务tab: 「同步ES/播放量」按钮
- OpsMonitor.vue 健康tab: overall 状态标签 (healthy/degraded)
- 新增 fetchQueueStats / triggerSync 函数

## 配置与发布模块：真正的「配置新模块并发布上线」流程（2026-06-26 修正）

### 之前问题
用户反馈：GitHub 推送不是他们要的。他们要的是「在这个模块里配置新模块/新功能开关，然后一键发布上线让配置生效」。

### 修正后的完整流程
1. **功能开关 tab**：新建/编辑/删除 Feature Flag，实时 toggle 开关状态
2. **版本发布 tab**：
   - 「新建发布」→ 自动**快照当前所有 Feature Flag 状态** → 存为 draft
   - 「部署上线」→ 读取快照 → **逐条 apply 到 live DB** → 当前线上版本切换为 deployed
   - 「导出」→ 下载 JSON 快照
   - 「查看快照」→ 弹窗预览所有开关状态

### 具体变更
- `AdminCreateRelease`：创建时自动调用 `buildConfigSnapshot()` 生成快照，状态 `"draft"`
- `AdminDeployRelease` `POST /config/releases/:id/deploy`：解析快照 JSON → 遍历 `feature_flags[]` → 逐条 UPDATE 到 DB（enabled/rollout_pct/whitelist）→ 旧 `deployed` 标记 `rolled_back` → 当前置为 `deployed`
- 前端：按钮从「发布(GitHub)」改为「部署上线」；列表显示部署状态（草稿/已部署/已回退）
- 回滚：`POST /config/releases/:id/rollback` 仍保留

### 状态流转
draft → deployed（部署上线时）
deployed → rolled_back（新版本部署或手动回滚时）

### 删除内容
- 移除 pushToGitHub() 函数及相关 GitHub API 调用
- 移除 bytes/base64/io/os imports
- 移除 .env 中 GitHub 配置注释
- 修正 AdminRollbackRelease：回滚时重新 apply 快照配置 + 状态更新为 deployed/rolled_back

## 首页右侧小电视：小心心图标 + 配置发布全流程（2026-06-26 第二次修正）

### 小心心图标
- elevator.vue: app-download 下方 `.heart-btn`，始终显示
- 点击切换 🤍↔❤️，`heartBeat` keyframe 弹性脉冲动画
- 已连接功能开关：`mounted()` 内 check `heart_anim_enabled`，返回 false 才隐藏
- 接口挂了保持显示（不打断 UI）

### 模块注册一键向导
- ConfigManage.vue 新增「模块注册」tab
- 填：Key + 描述 + 位置 + 版本号 → 点「一键注册并上线」
- 自动走完 3 步：创建 Flag → 新建 Release（快照）→ 部署上线
- 带进度步骤显示（✓/✗/正在…）
